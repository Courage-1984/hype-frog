Run reporter layer tests with concise output:

```powershell
uv run pytest tests/reporter/ -q --tb=short
```

Focus on TOC alignment, sheet names, sanitization, and Content Hub conditional formatting regressions.
