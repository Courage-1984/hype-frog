[pytest]
testpaths = tests
pythonpath = src
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    network: marks tests that perform live network or browser I/O
