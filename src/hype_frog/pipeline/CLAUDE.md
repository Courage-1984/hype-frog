# pipeline/ — scoped context

Inherits root [`CLAUDE.md`](../../../CLAUDE.md).

**Rule:** [`.claude/rules/pipeline.md`](../../../.claude/rules/pipeline.md).

Enrichment glue and Main merge. No `print()`. Assembler ownership stays in `crawler/data_assembler*`.
