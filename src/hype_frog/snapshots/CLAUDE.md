# snapshots/ — scoped context

Inherits root [`CLAUDE.md`](../../../CLAUDE.md).

**Rule:** [`.claude/rules/snapshots.md`](../../../.claude/rules/snapshots.md).

Crawl-replay store for `--regen-report`. Bump `CRAWL_SNAPSHOT_SCHEMA_VERSION` on breaking payload changes.
